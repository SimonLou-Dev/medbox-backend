name: Node.js prod CI
on:
  push:
    branches: ['prod', 'dev', 'testing']
  pull_request:
    branches: ['prod', 'dev', 'testing']
jobs:
  docker:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/docker.yaml
  